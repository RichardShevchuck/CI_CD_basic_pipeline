stages:
  - build

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == "main"


build-job:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "Building the application..."
    - docker build -t app:latest .
